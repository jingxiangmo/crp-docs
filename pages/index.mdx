import { Steps } from 'nextra/components'
import { Callout } from 'nextra/components'
import Link from 'next/link'


<Callout type="warning" emoji="⚠️">
    CRP is Work in Progress.
</Callout>

# The best way to start your ROS project


We made `create-ros-project` CLI to streamline the setup of ROS projects without compromising modularity.

After countless projects, we have done our best to standardize the setup of ROS projects into this CLI tool.

This is NOT an all-inclusive template. This is for starting new ROS projects and deploying your ROS project to robots.

<br/> <br/>


<img src="https://cln.sh/xtYr5V8D/download" alt="no alt" className="rounded-3xl shadow-xl"/>


<br/> <br/>

#### The best of the ROS ecosystem...
## But ONLY the parts you need
Take what you need and nothing more!

<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mt-6">
  {[
    { name: "Gazebo", description: "Robotics simulator for testing and development." },
    { name: "MoveIt", description: "Motion planning and manipulation." },
    { name: "RVIZ", description: "3D visualization of robot models and sensor data." },
    { name: "RQT", description: "Graphical user interface development." },
    { name: "Nav2", description: "Navigation stack for autonomous robots." },
    { name: "ros_control", description: "Controller management and hardware abstraction." },
  ].map((tool) => (
    <div key={tool.name} className="flex flex-col items-center p-4 rounded-lg shadow-md transition-all hover:shadow-lg">
      <img src={`/${tool.name.toLowerCase()}.png`} alt={`${tool.name} Logo`} className="h-16 mb-4"/>
      <h3 className="text-lg font-semibold mb-2 text-gray-800">{tool.name}</h3>
      <p className="text-sm text-gray-600 text-center">{tool.description}</p>
    </div>
  ))}
</div>


## Get Started
<Steps>

    ### Step 1: install and build from source

    ```bash copy
    git clone https://github.com/jingxiangmo/create-ros-project && cd create-ros-project && go build .
    # add to path
    echo "export PATH=\$PATH:$(pwd)" >> ~/.bashrc
    source ~/.bashrc
    ```

    ### Step 2: run the CLI application

    Option A: from the project directory

    ```bash copy
    # navigate to the directory containing the executable
    ./create-ros-project
    ```

    Option B: if the executable is in your PATH
    ```
    create-ros-project
    ```
</Steps>
